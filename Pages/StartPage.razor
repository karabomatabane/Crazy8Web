@page "/start"
<div class="start-container">

    @if (Owner == null)
    {
        <div class="player-form">
            <div class="form-card">
                <MatH3 class="form-title">Player Setup</MatH3>
                <div class="form-content">
                    <MatTextField
                        @bind-Value="@_inputName"
                        Label="Your Name"
                        FullWidth="true"
                        Outlined="true"
                        Class="name-input">
                    </MatTextField>
                    <div class="form-actions">
                        <MatButton
                            Raised="true"
                            Class="primary-button"
                            Disabled="@string.IsNullOrEmpty(_inputName)"
                            @onclick="CreatePlayer">
                            Continue
                        </MatButton>
                    </div>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="welcome-screen">
            <div class="welcome-header">
                <MatH3 class="welcome-title">Welcome back, <span class="player-name">@Owner.Name</span></MatH3>
                <p class="welcome-subtitle">What would you like to do today?</p>
            </div>

            <div class="action-buttons">
                <Button Raised="true" Class="action-button" @onclick="CreateGame">
                    <span class="button-icon">
                        <MatIcon Icon="videogame_asset"></MatIcon>
                    </span>
                    <span>Create New Game</span>
                </Button>

                <Button Raised="true" Class="action-button" @onclick="PrepareToJoin">
                    <span class="button-icon">
                        <MatIcon Icon="group"></MatIcon>
                    </span>
                    <span>Join Existing Game</span>
                </Button>

                <Button Raised="true" Class="action-button secondary" @onclick="ChangeName">
                    <span class="button-icon">
                        <MatIcon Icon="edit"></MatIcon>
                    </span>
                    <span>Change Name</span>
                </Button>
            </div>
        </div>
    }
</div>